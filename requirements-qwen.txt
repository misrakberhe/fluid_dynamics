# Qwen anchoring replication (Phase 1+)
# RunPod image runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404 ships torch 2.8 + CUDA 12.8.
# Do NOT pip-install torch on that image — setup_gpu_pod.sh uses --system-site-packages.
transformer-lens>=3
transformers>=5.4
tuned-lens
pandas
matplotlib
